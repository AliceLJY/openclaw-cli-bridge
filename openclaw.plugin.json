{
  "id": "cli-bridge",
  "name": "CLI Bridge",
  "version": "1.5.0",
  "description": "Docker-first command entry plane for OpenClaw-compatible local CLI execution",
  "author": "AliceLJY",
  "license": "MIT",
  "entry": "./index.ts",
  "configSchema": {
    "type": "object",
    "properties": {
      "apiUrl": {
        "type": "string",
        "default": "http://host.docker.internal:3456",
        "description": "Task API base URL (openclaw-worker endpoint)"
      },
      "apiToken": {
        "type": "string",
        "description": "Task API auth token (from openclaw-worker config)"
      },
      "callbackChannel": {
        "type": "string",
        "description": "Discord channel ID where CLI results are delivered"
      },
      "discordBotToken": {
        "type": "string",
        "description": "Discord bot token for this bot — CLI results will be sent using this identity"
      },
      "sessionStorePath": {
        "type": "string",
        "default": "/tmp/openclaw-cli-bridge-state.db",
        "description": "Path to persist CLI Bridge session mappings in SQLite across plugin restarts"
      }
    },
    "required": []
  }
}
